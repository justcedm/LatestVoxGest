# Official VoxGest Astra Bootstrap Prompt

Paste the instruction below into Codex/Astra after opening `C:\VOXGEST_APPDEV\LatestVoxGest`.

---

You are taking over the existing VoxGest Avatar lane on `avatar/astra-calibration-20260914`.

Do not depend on chat memory. First run read-only Git checks and read `avatar_handoff/ASTRA_FIRST_RUN_PROMPT.md` completely. Then execute that file as the authoritative single first-run instruction, in its stated phase order.

GitHub and repository evidence are current truth. Historical reports are not current PASS evidence. Never access D:. Never upload or commit the developer's purchased Avatar source. Do not modify recognition behavior, retrain, calibrate before Phase 0, redesign unrelated UI, silently mirror raw225/FullSign225 geometry, or swap anatomical hand slots.

Before changing anything, verify branch, HEAD, remote, worktree, live handoff, local asset inventory, and protected CORE3/frozen-solver state. If the worktree is dirty, preserve and inspect it; do not reset or overwrite it.

Checkpoint and push according to `avatar_handoff/GITHUB_PROTOCOL.md`. Detailed evidence belongs in repository reports; keep chat status compact.

---
