Dry-run assets are loaded from:

- `src/main/assets`
- repository `avatar/`
- repository `model/`

The dry-run app works without real animation curves. If a known word animation
JSON is missing or contains empty keyframes, the UI shows a clean placeholder
card instead.
